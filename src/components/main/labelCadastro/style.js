import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    textLabelCadastro:{
        color: "#FFFFFF",
        fontSize: 14,
        fontWeight: "bold",
        marginBottom:9,
    }
});

export default styles;