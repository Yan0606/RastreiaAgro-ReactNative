import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
   
    textTitle: {
        color: "#FFFFFF",
        fontSize: 14,
        fontWeight: "bold",
    }

});

export default styles;