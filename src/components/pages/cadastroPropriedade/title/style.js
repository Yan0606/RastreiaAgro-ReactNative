import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
   
    textTitle: {
        color: "#FFFFFF",
        fontSize: 16,
        width:337,
        left:20,
        marginBottom:20,
        fontWeight: "bold",
    }

});

export default styles;