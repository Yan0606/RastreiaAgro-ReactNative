import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    textLabelCadastro:{
        color: "#FFFFFF",
        fontSize: 15,
        marginBottom:9,
    }
});

export default styles;