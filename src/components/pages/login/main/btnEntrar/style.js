import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    button: {
        backgroundColor: "#009846", // Alterado para backgroundColor para definir a cor de fundo do botão
        justifyContent: "center",
        alignItems: "center",
        width: 340,
        height: 38,
        borderRadius: 40,
        marginBottom:24,
    },
    text: {
        color: "#FFFFFF",
        fontSize: 14,
        fontWeight: "bold",
    }
});

export default styles;