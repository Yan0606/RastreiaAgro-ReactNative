import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
   
    textTitle: {
        color: "#FFFFFF",
        fontSize: 20,
        width:337,
        left:10,
        marginBottom:20,
        fontWeight: "bold",
    }

});

export default styles;