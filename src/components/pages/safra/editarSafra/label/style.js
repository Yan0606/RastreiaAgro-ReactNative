import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

    label: {
        color: "#FFFFFF",
        fontSize: 16,  
        marginBottom:30,
        marginTop:30,
        marginRight:10,      
    }
});

export default styles;