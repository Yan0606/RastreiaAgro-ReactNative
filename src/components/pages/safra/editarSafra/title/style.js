import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

    textTitle: {
        color: "#FFFFFF",
        fontSize: 16,
        fontWeight: "bold",
        width:305,
        left: 20,
        marginBottom: 20,
    }
});

export default styles;